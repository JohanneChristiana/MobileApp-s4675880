<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/open"
        android:label="@string/app_name"
        android:roundIcon="@drawable/open"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileApps4675880"
        tools:targetApi="34">
        <!-- Course Activity -->
        <activity
            android:name=".CourseActivity"
            android:exported="false" />
        <!-- Second Screen Activity -->
        <activity
            android:name=".SecondScreenActivity"
            android:exported="false" />
        <!-- Second Screen Fragment -->
        <activity
            android:name=".SecondScreenFragment"
            android:exported="false" />
        <!-- Second Screen ViewModel -->
        <activity
            android:name=".SecondScreenViewModel"
            android:exported="false" />
        <!-- Main Screen Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>